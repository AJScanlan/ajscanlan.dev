---
import Nav from '../components/Nav.astro';

const {
    title = "ajscanlan.dev",
    description = "Working notebook — engineering, writing, and the space between.",
    ogImage,
} = Astro.props;
---

<html lang="en" class="scroll-smooth">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" href="/icons/favicon.svg" />
        <title>{title}</title>
        <meta name="description" content={description} />
        {ogImage && <meta property="og:image" content={ogImage} />}
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;500;600&display=swap" rel="stylesheet" />
        <style is:global>
            @import "/src/styles/global.css";
        </style>
    </head>
    <body>
        <a href="#main-content" class="skip-to-content">
            Skip to main content
        </a>

        <Nav />

        <main id="main-content" role="main">
            <slot />
        </main>

        <footer role="contentinfo" class="mt-20 border-t border-ink-100 py-8 font-mono text-sm text-ink-400">
            <div class="layout-home">
                <p>© {new Date().getFullYear()} A.J. Scanlan. Built with Astro.</p>
            </div>
        </footer>
    </body>
</html>
